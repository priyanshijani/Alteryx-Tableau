# Project Plan — Sales Pipeline Intelligence for a European IT Consultancy
**Alteryx (data prep & business rules) → Tableau (executive dashboards) → AI (grounded vs. ungrounded test)**

> Status: v1.0 (locked with stakeholder, 24 Sep 2026) · Owner: *you* · Reviewer: Claude (brainstorm partner)
> All data is synthetic and generated by `scripts/generate_raw_data.py`. Company: **Nexora Consulting** (fictional).

---

## 1. The business problem (why this project exists)

Nexora Consulting sells IT services (Cloud, Data & AI, Cybersecurity, ERP) to companies across Europe.
The CRM is messy: a system migration in July 2025 changed field names and formats, reps type stages and
countries in different ways, accounts are duplicated, and amounts come in eight currencies.

The **Chief Revenue Officer (CRO)** asks four questions every Monday:

| # | Executive question | Dashboard page |
|---|---|---|
| 1 | Will we hit next quarter's number, and how healthy is the pipeline? | **Pipeline Health & Forecast** |
| 2 | Where are deals getting stuck, and how much money is at risk? | **Deal Velocity & Stuck Deals** |
| 3 | Which managers, teams and departments convert pipeline into revenue? | **Manager & Department Performance** |
| 4 | Where in Europe (country, industry, service) should we invest next? | **Europe Market Opportunity** |
| + | Can I trust these numbers? | **Data Trust** (small page) |

**The AI gap this project demonstrates:** an AI assistant pointed at the raw CRM export will answer these
questions quickly and confidently, but wrongly — it double-counts duplicates, adds GBP to EUR, treats
"Proposal Sent" and "3 - Proposal" as different stages, and doesn't know that Nexora forecasts with
*governed* stage probabilities rather than what reps type in. This project writes those business rules down
once (Alteryx + `business_definitions.md`), proves the numbers reconcile to the CRM, and then shows the
difference it makes when an AI is grounded in them.

---

## 2. Scope

**In scope:** 24 months of CRM data (deals created Oct 2024 – Sep 2026, plus the CRM go-live import of deals
created Apr–Sep 2024), snapshot date **15 Sep 2026**. 18 European countries, 7 sales regions, 4 departments,
8 sales managers, 29 account executives, ~720 client accounts, ~4,800 opportunities.

**Out of scope:** marketing leads before they become opportunities, invoicing/revenue recognition,
live CRM connection (we work from exports, as most analysts do).

**Tools (all free):** Alteryx Designer (SparkED 90-day learner licence) · Tableau Desktop Free Edition
(optional: Tableau Public for a shareable link) · Git + GitHub · a free AI chat assistant.

---

## 3. Source data (what lands in `data/raw/`)

| File | What it is | Grain | Main problems planted |
|---|---|---|---|
| `crm_exports/opportunities/opps_YYYY_MM.csv` (24 files) | Monthly CRM extracts, one per creation month. `opps_2024_10.csv` also carries the go-live import. | 1 row per opportunity *version* | **Schema drift** from `opps_2025_07` (renamed & reordered columns, owner email instead of name, new `Forecast_Category`, no account name) · IDs `OPP-1234` vs `OPP-001234` · 29 stage spellings · mixed date formats (ISO, DD/MM/YYYY, DD.MM.YYYY) · amounts as `125000`, `125,000.00`, `€125,000`, `125 000` · currency `eur`/`€`/blank · probability as `75`, `75%`, `0.75` · exact duplicate rows · older versions of the same deal · 6 test records · 10 deals pointing to deleted accounts · 3 typo amounts (×100) · 4 negative amounts · blank amounts on early deals · 48 won deals without a close date |
| `crm_exports/stage_history_log.csv` | CRM audit log of field changes | 1 row per field change | Other fields mixed in (Amount, Close Date, Next Step) · 3 timestamp formats · ID variants (`opp-000123`, spaces, no padding) · duplicates that only look identical *after* parsing · history of deleted and test deals |
| `crm_exports/accounts_export.xlsx` | Excel report from CRM | 1 row per account record | 3 junk rows above the header, 2 footer rows · 45 duplicate accounts (case, spaces, missing legal suffix) · 59 country spellings · 29 industry labels · size bands written 14 ways · missing / swapped latitude-longitude · `n/a` revenue |
| `crm_exports/sales_team_roster.csv` | Sales Ops / HR roster | 1 row per person | Department spellings · trailing spaces in names · 2 leavers (one still owns open deals) · managers and AEs in one list |
| `reference/fx_rates_monthly.csv` | Finance FX table (clean) | month × currency | — |
| `reference/department_targets_quarterly.csv` | Finance bookings targets (clean) | department × quarter | — |
| `reference/crm_field_mapping_v1_v2.csv` | IT's migration mapping | 1 row per source field | — (used by the batch macro) |
| `reference/stage_rules.csv` | **Governed** stage rules (probabilities, stuck thresholds, forecast category) | 1 row per stage | — (business logic stored as data, not hard-coded) |
| `reference/country_region_mapping.csv` | Sales territory definition | 1 row per country | — |
| `reference/value_aliases.csv` | **You build this** while profiling: raw spelling → standard value | domain × raw value | Starter rows only |
| `reference/crm_control_totals.csv` | Counts and sums reported by the CRM itself | — | — (used to reconcile) |

---

## 4. Target data model (what Alteryx writes to `data/output/`)

Principle: **row-level business logic lives in Alteryx; Tableau only aggregates.** Every rule is written once,
documented in `docs/business_definitions.md`, and reused by dashboards and AI.

| Output (Hyper) | Grain | Purpose |
|---|---|---|
| `pipeline_deals.hyper` | 1 row per opportunity (latest version) | Main fact table for all pages |
| `pipeline_stage_stints.hyper` | 1 row per time a deal *sat in* an open stage | Velocity, days-in-stage, regressions |
| `sales_team.hyper` | 1 row per account executive | Quota, manager, active/left status |
| `dept_quarter_summary.hyper` | department × quarter | Targets vs bookings vs pipeline expected to close |
| `dq_reconciliation.hyper` (+ `.csv` in `quality/`) | 1 row per check | Data Trust page and README evidence |
| `quality/rejected_records.csv` | 1 row per removed record + reason | Audit trail |
| `model_scores.hyper` | 1 row per open opportunity | ML win probability, model-weighted €, review flag (Phase 6b) |
| `model_calibration.hyper` · `model_backtest.hyper` | probability bin × method · method | Proof the model beats governed probabilities |
| `ai/pipeline_deals_for_ai.csv` and `ai/raw_union_uncleaned.csv` | — | Inputs for the AI experiment |

### 4.1 `pipeline_deals` — column list

| Group | Columns |
|---|---|
| Keys | `opportunity_id`, `opportunity_name`, `master_account_id` |
| Account | `account_name`, `industry`, `company_size_band`, `country_code`, `country_name`, `sales_region`, `city`, `latitude`, `longitude`, `account_status` (Active Customer / Lapsed Customer / Prospect) |
| People | `owner_email`, `owner_name`, `sales_manager`, `department`, `owner_is_active` |
| Deal | `service_line`, `lead_source`, `deal_type` (New Logo / Expansion), `loss_reason`, `lost_at_stage` |
| Stage | `stage`, `stage_order`, `confidence_band`, `forecast_category`, `is_open`, `is_won`, `is_lost`, `max_stage_reached` |
| Money | `amount_local`, `currency`, `fx_rate`, `amount_eur`, `governed_probability`, `weighted_amount_eur`, `rep_probability`, `probability_gap_pts`, `deal_size_band` |
| Dates | `created_date`, `expected_close_date`, `actual_close_date`, `last_activity_date`, `stage_entered_date`, `close_quarter`, `expected_close_quarter` |
| Velocity & risk | `days_in_current_stage`, `stuck_after_days`, `is_stuck`, `days_since_activity`, `is_stale`, `is_past_due`, `regression_count`, `sales_cycle_days`, `is_at_risk`, `risk_reasons` |
| Data quality | `dq_flags` (e.g. `AMOUNT_MISSING;OWNER_LEFT`), `crm_version`, `source_file`, `snapshot_date` |

---

## 5. Alteryx design

```
alteryx/
├── 01_ingest_crm_extracts.yxmd        # Directory → batch macro → unified raw opportunities (.yxdb)
├── 02_build_pipeline_mart.yxmd        # all cleaning, rules, reconciliation, outputs
├── 03_win_probability_model.yxmd     # leakage-safe ML: photos, backtest, train, score
└── macros/
    ├── mc_ingest_crm_extract.yxmc     # BATCH macro: read 1 file, rename via mapping, tag source
    └── mc_normalize_company_name.yxmc # STANDARD macro (stretch): reusable name-normalisation key
```

`02_build_pipeline_mart.yxmd` is organised in **tool containers** (colour-coded, each with a comment):

| Container | What happens | Key tools / concepts |
|---|---|---|
| A · Sales team | Clean roster, standardise department, parse dates, active flag | Select, Data Cleansing, Find Replace, Formula |
| B · Accounts | Skip junk rows, drop footer, standardise country/industry/size, dedupe accounts to masters, fix lat/long | Input (start on line 4), Filter, Find Replace, Formula (REGEX), Summarize, Join |
| C · Opportunities | Standard IDs, remove exact duplicates & test records, keep latest version, parse dates/amounts/probabilities, map stages, resolve owners, map accounts | Unique, Sort, Multi-Field Formula, Formula, Find Replace, Join (**check L/R outputs**) |
| D · Stage history → stints | Keep stage rows, parse timestamps, dedupe after parsing, remove orphans, build entered/exited times, days in stage, regressions | Filter, Formula, Unique, Sort, **Multi-Row Formula** (group by opportunity), Summarize |
| E · Business rules | Governed probability, weighted €, FX conversion, stuck/stale/past-due, at-risk, deal type, account status, deal size band | Join to `stage_rules` & FX, Formula, Summarize, Workflow constant `SnapshotDate` |
| F · Reconciliation | Compare to CRM control totals, row bridge, stage counts, currency sums, orphan counts, fail loudly if broken | Summarize, Join, Formula, **Test** tool, Message tool |
| G · Outputs | Write Hyper files, CSVs for AI and audit | Output Data (.hyper / .csv), Select (final names & types) |

`03_win_probability_model.yxmd` (Phase 6b) has five containers: **H1** photos of closed deals (Append Fields × checkpoint days), **H2** time-based backtest split at 1 Mar 2026, **H3** Logistic Regression + Forest Model, **H4** Score + Brier + calibration + business backtest, **H5** retrain on all history and score today's open deals. Documented in `docs/model_card.md`.

**Design rules to show off in the README:**
1. Business rules stored as reference data (`stage_rules.csv`, `value_aliases.csv`), not buried in formulas.
2. Every Join's unmatched (L/R) output is inspected, counted and either fixed or logged.
3. Nothing is silently deleted: removed rows go to `rejected_records.csv` with a reason.
4. The workflow reconciles to CRM control totals and stops with an error if it doesn't.
5. One workflow constant (`SnapshotDate = 2026-09-15`) drives every "as of" calculation.

---

## 6. Tableau design

**Canvas:** fixed 1400 × 900 px (consistent screenshots). **Style:** dark navy title bar, white background,
one accent colour per meaning: Won = green, Lost = grey, open stages = sequential blues, risk = amber/red.
Every page has: title that states the question, 4–6 KPI tiles, 3–4 charts, filters on the right,
a one-line "So what" text box written by you.

**Data sources:** the Hyper files, joined with **relationships** — `pipeline_deals` ↔ `pipeline_stage_stints`
(opportunity_id), `pipeline_deals` ↔ `sales_team` (owner_email). `dept_quarter_summary` is a separate
source filtered with the shared `department` field.

### Page 1 — Pipeline Health & Forecast (main page)
- KPI tiles: Open Pipeline €, Weighted Pipeline €, Q3-2026 Bookings vs Target %, Next-Quarter (Q4-2026) Coverage ×, Win Rate (last 12 months), Average Won Deal €.
- Open pipeline funnel by stage (count and €) with stage-to-stage conversion %.
- Forecast by expected close quarter: stacked bar (Commit / Best Case / Pipeline, weighted €) with target reference line.
- Monthly bookings trend vs quarterly target.
- Filters: Department, Sales Region, Service Line.

### Page 2 — Deal Velocity & Stuck Deals
- KPI tiles: € At Risk, # Stuck Deals, Average Sales Cycle (won, days), # Past-Due Close Dates.
- Median days in stage vs stuck threshold (reference line), by stage; heatmap department × stage.
- Scatter: days in current stage vs deal €, coloured by risk reason.
- "Fix-this-week" table: top 15 at-risk deals (owner, stage, days stuck, last activity, €, reasons).
- Regressions (deals that moved backwards) by department.

### Page 3 — Manager & Department Performance
- Department → Manager → AE drill-down: FY2026 bookings, win rate, open pipeline, average cycle.
- Bullet chart: AE quota attainment year-to-date (prorated quota).
- Scatter per manager: open pipeline € (x) vs win rate (y), bubble = bookings.
- Forecast honesty: average gap between rep-entered probability and governed probability, by manager.
- Loss reasons by department (100% stacked bar).

### Page 4 — Europe Market Opportunity
- Map: countries filled by open pipeline €, dual-axis city bubbles (size = pipeline, colour = industry).
- Heatmap industry × sales region: colour = win rate, label = pipeline €.
- Pipeline by service line for the selected region (map acts as a filter).
- Top accounts table with account status (Active Customer / Prospect).

### Page 5 — Win Probability (ML)
- Calibration chart: predicted vs actual win rate by probability bin, model vs governed, with a 45° reference line.
- Backtest: governed forecast € vs model forecast € vs actual won € for the pipeline of 1 Mar 2026.
- "Gut vs rules vs data": average rep, governed and model probability by manager.
- Open-deal review list: deals where model and governed probability differ by ≥ 25 points.
- (Page 1 also gets an *advisory* Model-weighted Pipeline € tile.)

### Page 6 — Data Trust (small)
- Row bridge: raw rows → duplicates → test → older versions → final deals (waterfall or bar).
- Reconciliation checks table (pass/fail) and counts of records auto-fixed by each rule.

**Tableau techniques to showcase:** relationships, LOD expressions, table calculations (% of total,
running sum), parameters (Top N, € vs count toggle), dual-axis map, filter/highlight actions,
navigation buttons between pages, viz-in-tooltip.

---

## 7. AI experiment design (the differentiator)

Same five questions, asked twice to a free AI assistant, compared with the Tableau "ground truth":

| Round | What the AI gets | Expected outcome |
|---|---|---|
| A · Ungrounded | `raw_union_uncleaned.csv` | Fast, confident, and wrong in specific, explainable ways |
| B · Grounded | `pipeline_deals_for_ai.csv` + `business_definitions.md` | Matches Tableau (or close) |

The ML model and the AI assistant play different roles: the **model** produces a number (win probability) from history; the **AI assistant** explains and summarises numbers. Both are only trustworthy on top of governed, reconciled data.
| C · Narrative | Round B context + "write the CRO's Monday summary" | You verify every claim against Tableau and log it |

Full protocol in `ai_experiment/README.md`.

---

## 8. Milestones

| Week | Milestone | Done when |
|---|---|---|
| 1 | Setup + profiling + batch macro | `01_ingest` produces 5,012 unified rows from 24 files |
| 2 | Accounts, team, opportunities cleaned | 720 master accounts, 4,800 deals, all joins' L/R outputs explained |
| 3 | Stage stints + business rules | Stints and rules match the answer key |
| 3–4 | ML win probability (Phase 6b) + reconciliation + outputs | Model beats governed probabilities on backtest; all checks pass; Hyper files written |
| 4 | Tableau pages 1–2 | Screenshots saved |
| 5 | Tableau pages 3–6 | Screenshots saved |
| 6 | AI experiment, README, GitHub, LinkedIn | Repo public, post published |

---

## 9. Planted stories (spoiler — read after you build the dashboards)

<details>
<summary>Click to reveal what the data was designed to show</summary>

- **Data & AI** creates the most pipeline and is growing fastest, but deals sit ~50% longer in **Proposal** (GenAI budgets awaiting approval).
- One Data & AI manager's team (West & South) creates lots of pipeline, converts poorly, and enters probabilities well above the governed ones.
- A small **Cybersecurity** team in North & Central Europe has the best win rate in the company.
- **Nordics, DACH and CEE** convert better than **Southern Europe and UK & Ireland**; Southern Europe cycles are longer.
- **Public Sector** stalls in **Negotiation** (procurement) and in Solution Design.
- **Energy & Utilities** (energy transition) is a high-converting industry, concentrated in DACH and the Nordics.
- About a third of open pipeline breaches a stuck threshold — pipeline hygiene is itself an executive insight.
- A departed rep still owns open deals (orphaned pipeline).
- The governed stage probabilities are **too optimistic**: on the 1 Mar 2026 backtest they overstate won € much more than the ML model does, which is a reason to recalibrate `stage_rules.csv`.

</details>

---

## 10. Answer key for self-checks

Use these to validate your Alteryx work (they are also what a CRM control report would tell you).

| Check | Expected |
|---|---|
| Rows after unioning 24 opportunity files | 5,012 |
| Exact duplicate rows (same file, all fields) | 75 |
| Test records | 6 |
| Older versions of the same opportunity | 131 |
| Final unique opportunities | **4,800** (= CRM 4,806 − 6 test) |
| Stage counts (Discovery / Solution Design / Proposal / Negotiation / Won / Lost) | 233 / 218 / 194 / 79 / 1,107 / 2,969 |
| Open deals | 724 |
| Deals with blank amount | 44 |
| Won deals without a close date | 48 (fill from stage history) |
| Deals pointing to deleted accounts | 10 |
| Account records → master accounts | 765 → 720 (45 duplicates) |
| Stage-change rows (Field = Stage) | 16,289 → 16,129 after de-duplicating *parsed* values → 16,093 after removing 36 orphan rows |
| Open-stage stints | 12,017 |
| Deals that moved backwards at least once | 206 |

| ML backtest: training photos / deals (closed before 1 Mar 2026) | ≈ 20,000 / ≈ 2,900 |
| ML backtest: deals open on 1 Mar 2026 / decided by 15 Sep | ≈ 635 / ≈ 550 |
| ML backtest: AUC model vs governed · Brier model vs governed | ≈ 0.72–0.76 vs ≈ 0.67 · ≈ 0.20 vs ≈ 0.26 |
| ML final training photos / deals | ≈ 29,000 / ≈ 4,070 |
| Open deals scored / not scored (no amount) | ≈ 690 / ≈ 32 |

A complete alias table is in `docs/solutions/value_aliases_complete.csv` — build your own first, then compare.
